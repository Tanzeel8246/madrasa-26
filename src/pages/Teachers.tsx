import { Layout } from '@/components/Layout/Layout';

const Teachers = () => {
  return (
    <Layout>
      <h1 className="text-3xl font-bold">اساتذہ</h1>
    </Layout>
  );
};

export default Teachers;
