import { Layout } from '@/components/Layout/Layout';

const Courses = () => {
  return (
    <Layout>
      <h1 className="text-3xl font-bold">نصاب</h1>
    </Layout>
  );
};

export default Courses;
