import { Layout } from '@/components/Layout/Layout';

const Settings = () => {
  return (
    <Layout>
      <h1 className="text-3xl font-bold">ترتیبات</h1>
    </Layout>
  );
};

export default Settings;
